-- ============================================================
--  접수과 메모: 환자에게 영구적으로 붙는 메모 (내원별 메모와 별개)
--  예) "한국말 못함", "보호자 동반 필요" 등. (재실행 안전)
-- ============================================================
ALTER TABLE patient ADD COLUMN IF NOT EXISTS reception_note TEXT;
